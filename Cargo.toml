[package]
name = "dcos-sdk-service-diagnostics"
version = "0.1.0"
authors = ["Mesosphere <users@dcos.io>"]
edition = "2018"

[dependencies]
structopt = "0.2"
# failure = "0.1.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"